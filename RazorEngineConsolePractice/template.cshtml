<!DOCTYPE html>
                                <html>
                                <head>
                                    <title>@Model.Title @Model.Name</title>
                                </head>
                                <body>
                                    <h1>Hello, @Model.Title @Model.Name!</h1>
                                    <p>Age: @Model.Age</p>
                                </body>
                                </html>